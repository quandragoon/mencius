function ServerHandler(ip) {

	this.serverIP = ip;

	// Sends move (string) to server
	this.sendMove = function(move) {
		
	};

	// Ping server for updated game state
	this.getUpdatedGameState = function(callback) {

	};
}
